# SPDX-FileCopyrightText: 2025 Slavi Pantaleev
# SPDX-FileCopyrightText: 2025 spatterlight
# SPDX-FileCopyrightText: 2026 sudo-Tiz
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Ensure qBittorrent paths exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0750"
    owner: "{{ qbittorrent_uid }}"
    group: "{{ qbittorrent_gid }}"
  with_items:
    - "{{ qbittorrent_base_path }}"
    - "{{ qbittorrent_data_path }}"
    - "{{ qbittorrent_config_path }}"
    - "{{ qbittorrent_download_path }}"

- name: Ensure qBittorrent support files installed
  ansible.builtin.template:
    src: "{{ role_path }}/templates/{{ item }}.j2"
    dest: "{{ qbittorrent_base_path }}/{{ item }}"
    mode: "0640"
    owner: "{{ qbittorrent_uid }}"
    group: "{{ qbittorrent_gid }}"
  with_items:
    - env
    - labels
  register: qbittorrent_support_files_result

- name: Ensure qBittorrent container image is pulled via community.docker.docker_image_pull
  when: devture_systemd_docker_base_container_image_pull_method == 'ansible-module'
  community.docker.docker_image_pull:
    name: "{{ qbittorrent_container_image }}"
    pull: always
  register: qbittorrent_container_image_pull_result_ansible_module
  retries: "{{ devture_playbook_help_container_retries_count }}"
  delay: "{{ devture_playbook_help_container_retries_delay }}"
  until: qbittorrent_container_image_pull_result_ansible_module is not failed

- name: Ensure qBittorrent container image is pulled via ansible.builtin.command
  when: devture_systemd_docker_base_container_image_pull_method == 'command'
  ansible.builtin.command:
    cmd: "{{ devture_systemd_docker_base_host_command_docker }} pull {{ qbittorrent_container_image }}"
  register: qbittorrent_container_image_pull_result_command
  retries: "{{ devture_playbook_help_container_retries_count }}"
  delay: "{{ devture_playbook_help_container_retries_delay }}"
  until: qbittorrent_container_image_pull_result_command is not failed
  changed_when: "'Downloaded newer image' in qbittorrent_container_image_pull_result_command.stdout"

- name: Ensure qBittorrent container network is created via community.docker.docker_network
  when: devture_systemd_docker_base_container_network_creation_method == 'ansible-module'
  community.docker.docker_network:
    enable_ipv6: "{{ devture_systemd_docker_base_ipv6_enabled }}"
    name: "{{ qbittorrent_container_network }}"
    driver: bridge
    driver_options: "{{ devture_systemd_docker_base_container_networks_driver_options }}"
  register: qbittorrent_network_creation_result

- name: Ensure qBittorrent container network is created via ansible.builtin.command
  when: devture_systemd_docker_base_container_network_creation_method == 'command'
  ansible.builtin.command:
    cmd: >-
      {{ devture_systemd_docker_base_host_command_docker }} network create
      {% if devture_systemd_docker_base_ipv6_enabled %}--ipv6{% endif %}
      {{ devture_systemd_docker_base_container_networks_driver_options_string }}
      {{ qbittorrent_container_network }}
  register: qbittorrent_network_creation_result
  changed_when: qbittorrent_network_creation_result.rc == 0
  failed_when: qbittorrent_network_creation_result.rc != 0 and 'already exists' not in qbittorrent_network_creation_result.stderr

- name: Ensure qBittorrent systemd service is present
  ansible.builtin.template:
    src: "{{ role_path }}/templates/systemd/qbittorrent.service.j2"
    dest: "{{ devture_systemd_docker_base_systemd_path }}/{{ qbittorrent_identifier }}.service"
    mode: "0644"
  register: qbittorrent_systemd_service_result

# qBittorrent reads `qBittorrent.conf` at startup, keeps all settings in memory
# and rewrites the whole file from memory on a clean shutdown.
# Modifying the file while the service is running is therefore pointless -
# the changes get discarded the moment the service is stopped.
#
# We thus only touch the file while the service is stopped, restoring the
# service to the state it was in afterwards.
- name: Ensure qBittorrent configuration overrides are applied
  when: "qbittorrent_config_overrides | length > 0"
  block:
    # See the note about `mode` below.
    - name: Check if qBittorrent configuration override options need applying  # noqa risky-file-permissions
      community.general.ini_file:
        path: "{{ qbittorrent_config_file_path }}"
        section: "{{ item.section }}"
        option: "{{ item.option }}"
        value: "{{ item.value }}"
        create: true
        no_extra_spaces: true
        owner: "{{ qbittorrent_uid }}"
        group: "{{ qbittorrent_gid }}"
      check_mode: true
      loop: "{{ qbittorrent_config_overrides }}"
      loop_control:
        label: "[{{ item.section }}] {{ item.option }}"
      register: qbittorrent_config_overrides_check_result

    - name: Determine whether qBittorrent configuration overrides need applying
      ansible.builtin.set_fact:
        qbittorrent_config_overrides_changes_necessary: "{{ qbittorrent_config_overrides_check_result.changed | default(false) }}"

    - name: Apply qBittorrent configuration overrides
      when: qbittorrent_config_overrides_changes_necessary | bool
      block:
        - name: Populate service facts
          ansible.builtin.service_facts:

        - name: Determine whether the qBittorrent systemd service is running
          ansible.builtin.set_fact:
            qbittorrent_service_was_running: "{{ ansible_facts.services[qbittorrent_identifier + '.service'] | default(none) is not none and ansible_facts.services[qbittorrent_identifier + '.service'].state == 'running' }}"

        - name: Ensure qBittorrent systemd service is stopped before applying configuration overrides
          when: qbittorrent_service_was_running | bool
          ansible.builtin.systemd_service:
            name: "{{ qbittorrent_identifier }}.service"
            state: stopped

        # We intentionally do not enforce a `mode` here (hence the `noqa` below).
        # qBittorrent recreates this file with its own permissions, so enforcing a mode
        # would make this task report a change on every playbook run.
        - name: Ensure qBittorrent configuration override options are applied  # noqa risky-file-permissions
          community.general.ini_file:
            path: "{{ qbittorrent_config_file_path }}"
            section: "{{ item.section }}"
            option: "{{ item.option }}"
            value: "{{ item.value }}"
            create: true
            no_extra_spaces: true
            owner: "{{ qbittorrent_uid }}"
            group: "{{ qbittorrent_gid }}"
          loop: "{{ qbittorrent_config_overrides }}"
          loop_control:
            label: "[{{ item.section }}] {{ item.option }}"

        - name: Ensure qBittorrent systemd service is started back after applying configuration overrides
          when: qbittorrent_service_was_running | bool
          ansible.builtin.systemd_service:
            name: "{{ qbittorrent_identifier }}.service"
            state: started

- name: Determine whether qBittorrent needs a restart
  ansible.builtin.set_fact:
    qbittorrent_restart_necessary: >-
      {{
        qbittorrent_support_files_result.changed | default(false)
        or qbittorrent_systemd_service_result.changed | default(false)
        or qbittorrent_container_image_pull_result_ansible_module.changed | default(false)
        or qbittorrent_container_image_pull_result_command.changed | default(false)
      }}
